[route]
method = "GET"
path = "/hints"
//[route] is a special Codicil convention which embeds TOML syntax in the .bv file to handle easy routing

defn handle() -> String [true][result != ""] {
    term "{\"hint\":\"Stay curious.\"}";
};